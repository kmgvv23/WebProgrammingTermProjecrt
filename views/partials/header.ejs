<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= (typeof pageTitle !== 'undefined' ? pageTitle : 'StudyLink') %> - STUDY LINK</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;700&display=swap" rel="stylesheet">
    <!-- 공통 CSS -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

<!-- === 상단 내비게이션 바 === -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">

        <!-- 로고 -->
        <a class="navbar-brand ms-1" href="/dashboard">
            <img src="/images/book-spells-white.png" alt="아이콘" class="icon-png-nav">
            STUDY LINK 
            <span class="brand-subtitle">—&ensp;온라인 독서 소모임</span>
        </a>

        <!-- 모바일 토글 버튼 (HEAD에서 가져옴) -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- 메뉴 -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item">
                    <a class="nav-link" href="/studies">FIND STUDY</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/studies/new">CREATE STUDY</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/mypage">MYPAGE</a>
                </li>

                <% if (typeof user !== 'undefined' && user) { %>
                    <li class="nav-item ms-lg-3">
                        <span class="text-light me-2 small">@<%= user.nickname %></span>
                    </li>
                    <li class="nav-item">
                        <form action="/auth/logout" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-outline-light">LOGOUT</button>
                        </form>
                    </li>
                <% } else { %>
                    <li class="nav-item ms-lg-3">
                        <a class="btn btn-sm btn-primary" href="/login">LOGIN</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<!-- === 본문 시작 === -->
<main class="container">
