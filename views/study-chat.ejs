<%- include('partials/header') %>

<style>
    /* KakaoTalk-style Chat Design */
    .chat-container-wrapper {
        max-width: 900px;
        margin: 0 auto;
    }

    .chat-header {
        background: linear-gradient(135deg, rgba(102, 126, 234, 0.1) 0%, rgba(118, 75, 162, 0.1) 100%);
        border-radius: 16px 16px 0 0;
        padding: 1.5rem 2rem;
        border: 1px solid rgba(102, 126, 234, 0.2);
        border-bottom: none;
    }

    .chat-header h2 {
        color: #2d3748;
        font-weight: 700;
        font-size: 1.5rem;
        margin: 0;
    }

    .chat-header p {
        color: #4a5568;
        font-weight: 500;
        margin: 0.5rem 0 0;
    }

    #chat-box {
        height: 500px;
        overflow-y: auto;
        background: linear-gradient(135deg, #f7fafc 0%, #edf2f7 100%);
        padding: 2rem;
        border-left: 1px solid rgba(102, 126, 234, 0.2);
        border-right: 1px solid rgba(102, 126, 234, 0.2);
    }

    /* 채팅 메시지 스타일 */
    .chat-message {
        display: flex;
        margin-bottom: 1.5rem;
        animation: messageSlideIn 0.3s ease-out;
    }

    @keyframes messageSlideIn {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* 상대방 메시지 (왼쪽) */
    .chat-message.other {
        flex-direction: row;
        justify-content: flex-start;
    }

    .chat-message.other .message-bubble {
        background: white;
        color: #2d3748;
        border-radius: 4px 16px 16px 16px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }

    /* 내 메시지 (오른쪽) */
    .chat-message.mine {
        flex-direction: row-reverse;
        justify-content: flex-start;
    }

    .chat-message.mine .message-bubble {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border-radius: 16px 4px 16px 16px;
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    .message-avatar {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: 700;
        font-size: 0.9rem;
        flex-shrink: 0;
        box-shadow: 0 2px 8px rgba(79, 172, 254, 0.3);
    }

    .chat-message.mine .message-avatar {
        background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
    }

    .message-content {
        max-width: 60%;
        margin: 0 0.75rem;
    }

    .message-bubble {
        padding: 0.875rem 1.25rem;
        word-wrap: break-word;
        line-height: 1.5;
    }

    .message-sender {
        font-size: 0.85rem;
        font-weight: 600;
        margin-bottom: 0.375rem;
        color: #4a5568;
    }

    .chat-message.mine .message-sender {
        text-align: right;
        color: #718096;
    }

    .message-time {
        font-size: 0.75rem;
        color: #a0aec0;
        margin-top: 0.25rem;
        display: flex;
        align-items: flex-end;
    }

    .chat-message.other .message-time {
        justify-content: flex-start;
    }

    .chat-message.mine .message-time {
        justify-content: flex-end;
    }

    /* 입력 폼 */
    #chat-form-container {
        background: white;
        border: 1px solid rgba(102, 126, 234, 0.2);
        border-top: 2px solid rgba(102, 126, 234, 0.3);
        border-radius: 0 0 16px 16px;
        padding: 1.25rem 2rem;
    }

    #chat-input {
        border: 2px solid #e2e8f0;
        border-radius: 50px;
        padding: 0.875rem 1.5rem;
        font-size: 1rem;
        transition: all 0.3s ease;
    }

    #chat-input:focus {
        border-color: #667eea;
        box-shadow: 0 0 0 4px rgba(102, 126, 234, 0.1);
        outline: none;
    }

    #chat-submit-btn {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border: none;
        border-radius: 50px;
        padding: 0.875rem 1.5rem;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
        white-space: nowrap;
        flex-shrink: 0;
    }

    #chat-submit-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(102, 126, 234, 0.4);
    }

    .nav-buttons {
        margin-top: 1.5rem;
        display: flex;
        gap: 1rem;
        justify-content: center;
    }

    .btn-nav-home {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border: none;
        padding: 0.75rem 1.5rem;
        font-weight: 600;
        border-radius: 12px;
        text-decoration: none;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
    }

    .btn-nav-home:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(102, 126, 234, 0.4);
        color: white;
    }

    .btn-nav-board {
        background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        color: white;
        border: none;
        padding: 0.75rem 1.5rem;
        font-weight: 600;
        border-radius: 12px;
        text-decoration: none;
        transition: all 0.3s ease;
        box-shadow: 0 4px 12px rgba(79, 172, 254, 0.3);
    }

    .btn-nav-board:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(79, 172, 254, 0.4);
        color: white;
    }

    .empty-chat {
        text-align: center;
        padding: 4rem 2rem;
        color: #718096;
    }

    .empty-chat svg {
        width: 80px;
        height: 80px;
        margin-bottom: 1.5rem;
        opacity: 0.3;
    }

    /* Custom Scrollbar */
    #chat-box::-webkit-scrollbar {
        width: 8px;
    }

    #chat-box::-webkit-scrollbar-track {
        background: rgba(0, 0, 0, 0.05);
        border-radius: 10px;
    }

    #chat-box::-webkit-scrollbar-thumb {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 10px;
    }
</style>

<div class="container mt-4 chat-container-wrapper">
    <div class="chat-header">
        <h2>💬 <%= study.title %> - 채팅방</h2>
        <p>스터디 멤버들과 실시간으로 대화해보세요</p>
    </div>

    <!-- 채팅 메시지 -->
    <div id="chat-box">
        <% if (messages && messages.length > 0) { %>
            <% messages.forEach(function(m) { %>
                <% const isMyMessage = user && m.userId && m.userId == user.id; %>
                <div class="chat-message <%= isMyMessage ? 'mine' : 'other' %>">
                    <div class="message-avatar">
                        <%= (m.nickname || '?')[0].toUpperCase() %>
                    </div>
                    <div class="message-content">
                        <% if (!isMyMessage) { %>
                            <div class="message-sender"><%= m.nickname || '알 수 없음' %></div>
                        <% } %>
                        <div class="message-bubble">
                            <%= m.message %>
                        </div>
                        <div class="message-time">
                            <%= m.createdAtFormatted %>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div class="empty-chat">
                <svg viewBox="0 0 24 24" fill="currentColor">
                    <path d="M20 2H4c-1.1 0-2 .9-2 2v18l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm0 14H6l-2 2V4h16v12z"/>
                </svg>
                <p style="font-size: 1.1rem; font-weight: 500;">아직 대화가 없습니다</p>
                <p>첫 메시지를 보내보세요!</p>
            </div>
        <% } %>
    </div>

    <!-- 입력 폼 -->
    <div id="chat-form-container">
        <form id="chat-form" class="d-flex gap-2">
            <input id="chat-input"
                   type="text"
                   class="form-control"
                   placeholder="메시지를 입력하세요..."
                   autocomplete="off" />
            <button id="chat-submit-btn" type="submit">
                <svg width="18" height="18" fill="currentColor" viewBox="0 0 16 16" style="margin-right: 4px; vertical-align: -3px;">
                    <path d="M15.854.146a.5.5 0 0 1 .11.54l-5.819 14.547a.75.75 0 0 1-1.329.124l-3.178-4.995L.643 7.184a.75.75 0 0 1 .124-1.33L15.314.037a.5.5 0 0 1 .54.11ZM6.636 10.07l2.761 4.338L14.13 2.576 6.636 10.07Zm6.787-8.201L1.591 6.602l4.339 2.76 7.494-7.493Z"/>
                </svg>
                전송
            </button>
        </form>
    </div>

    <!-- 네비게이션 버튼 -->
    <div class="nav-buttons">
        <a href="/studies/<%= study.id %>/room" class="btn-nav-home">
            <svg width="18" height="18" fill="currentColor" viewBox="0 0 16 16" style="margin-right: 6px; vertical-align: -3px;">
                <path d="M8.707 1.5a1 1 0 0 0-1.414 0L.646 8.146a.5.5 0 0 0 .708.708L2 8.207V13.5A1.5 1.5 0 0 0 3.5 15h9a1.5 1.5 0 0 0 1.5-1.5V8.207l.646.647a.5.5 0 0 0 .708-.708L13 5.793V2.5a.5.5 0 0 0-.5-.5h-1a.5.5 0 0 0-.5.5v1.293L8.707 1.5ZM13 7.207V13.5a.5.5 0 0 1-.5.5h-9a.5.5 0 0 1-.5-.5V7.207l5-5 5 5Z"/>
            </svg>
            스터디 홈으로
        </a>
        <a href="/studies/<%= study.id %>/board" class="btn-nav-board">
            <svg width="18" height="18" fill="currentColor" viewBox="0 0 16 16" style="margin-right: 6px; vertical-align: -3px;">
                <path d="M14 4.5V14a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V2a2 2 0 0 1 2-2h5.5L14 4.5zm-3 0A1.5 1.5 0 0 1 9.5 3V1H4a1 1 0 0 0-1 1v12a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1V4.5h-2z"/>
            </svg>
            게시판 바로가기
        </a>
    </div>
</div>

<!-- Socket.IO 클라이언트 -->
<script src="/socket.io/socket.io.js"></script>
<script>
    (function() {
        const studyId  = '<%= study.id %>';
        const userId   = "<%= user ? user.id : '' %>";
        const nickname = "<%= user ? user.nickname : '' %>";

        // 서버에 연결
        const socket = io({
            query: {
                studyId,
                userId,
                nickname
            }
        });

        const chatBox  = document.getElementById('chat-box');
        const chatForm = document.getElementById('chat-form');
        const chatInput= document.getElementById('chat-input');

        // 서버에서 온 메시지 표시
        socket.on('chatMessage', function(data) {
            const isMyMessage = data.userId == userId;
            const messageDiv = document.createElement('div');
            messageDiv.className = 'chat-message ' + (isMyMessage ? 'mine' : 'other');

            const avatar = document.createElement('div');
            avatar.className = 'message-avatar';
            avatar.textContent = (data.nickname || '?')[0].toUpperCase();

            const content = document.createElement('div');
            content.className = 'message-content';

            if (!isMyMessage) {
                const sender = document.createElement('div');
                sender.className = 'message-sender';
                sender.textContent = data.nickname || '알 수 없음';
                content.appendChild(sender);
            }

            const bubble = document.createElement('div');
            bubble.className = 'message-bubble';
            bubble.textContent = data.message;
            content.appendChild(bubble);

            const time = document.createElement('div');
            time.className = 'message-time';
            time.textContent = new Date(data.createdAt).toLocaleTimeString('ko-KR', {
                hour: '2-digit',
                minute: '2-digit'
            });
            content.appendChild(time);

            messageDiv.appendChild(avatar);
            messageDiv.appendChild(content);

            chatBox.appendChild(messageDiv);
            chatBox.scrollTop = chatBox.scrollHeight;
        });

        // 메시지 전송
        chatForm.addEventListener('submit', function(e) {
            e.preventDefault();
            const text = chatInput.value.trim();
            if (!text) return;
            socket.emit('chatMessage', text);
            chatInput.value = '';
        });

        // 페이지 로드 시 스크롤 최하단으로
        chatBox.scrollTop = chatBox.scrollHeight;
    })();
</script>

<%- include('partials/footer') %>
